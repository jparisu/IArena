"""Configuration for TicTacToe."""

from iarena.game.GameConfig import GameConfig


class TicTacToeConfig(GameConfig):
    """Static configuration for a standard 3x3 TicTacToe game.

    The standard game has exactly two players (X and O) on a 3x3 board.
    No variant parameters are needed for the classic version.
    """
