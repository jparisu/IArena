"""Defines the primitive player identifier type used in arenas."""


class PlayerIndex(int):
    """Primitive wrapper representing a player identifier in an arena.

    Purpose:
        Provides the `PlayerIndex` type within the IArena architecture.
    How it works:
        Encapsulates behavior through its public API and type contracts defined in this class body.
    Used for:
        Building game, mapping, utility, or protocol components that can be composed by other modules.
    Public Attributes:
        None declared at class level in this base definition.
    """
