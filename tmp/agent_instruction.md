task: develop

Goal:
- implement a new interface to play any game in terminal

Implement the interface to play a game in terminal. This submodule must implement the following:

- An `Arena` that manages the loop.
- A `TerminalPlayer`. This is a generic player that can be used to play any game in terminal. Move it from where it currently is.

Requirements:
- Implement each of its components in different files.
- Document extensively
- Test are not required

Output:
- `terminal` interface implementation
